# Regenerated by CI via `protoc --go_out=gen ...`. Treating this like
# prost-build's OUT_DIR on the Rust side — the generator is the source of
# truth, not the generated bytes. Local devs run the protoc command from
# compat-tests/README.md before `go run . generate`.
gen/
